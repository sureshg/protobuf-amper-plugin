product: jvm/amper-plugin

dependencies:
  # protobuf4j — pure JVM protoc via WASM (v3 and v4)
  - $libs.protobuf4j.v3
  - $libs.protobuf4j.v4
  # grpc-kotlin codegen — pure JVM, no native binary (jdk8 classifier not supported in version catalog)
  - io.grpc:protoc-gen-grpc-kotlin:1.5.0:jdk8

pluginInfo:
  settingsClass: Schema
